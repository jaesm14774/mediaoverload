schedules:
  # waddledee:
    # character: "Waddledee"
    # prompts:
      # - use_llm: true
        # character: "Waddledee"
        # temperature: 1.0 
  wobbuffet:
    character: "Wobbuffet"
    prompts:
      - use_llm: true
        prompt: ""
        temperature: 1.0
  # kirby:
    # character: "Kirby"
    # prompts:
      # - use_llm: true
        # character: "Kirby"
        # temperature: 1.0 