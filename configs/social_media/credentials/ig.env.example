# Instagram 帳號設定範例
# 複製此檔案為 ig.env 並填入實際的 Instagram 帳號資訊
# 注意：此檔案應放在 configs/social_media/credentials/{character_name}/ 目錄下

# Instagram 帳號資訊
IG_USERNAME=your_instagram_username
IG_PASSWORD=your_instagram_password
IG_USER_ID=your_instagram_user_id

# 可選的其他設定
# Cookie 檔案路徑（相對於 config_folder_path）
IG_ACCOUNT_COOKIE_FILE_PATH=ig_account.json

# Proxy 設定（可選，當 IP 被封鎖時使用）
# HTTP/HTTPS Proxy 格式：http://username:password@proxy_host:port 或 http://proxy_host:port
# SOCKS5 Proxy 格式：socks5://username:password@proxy_host:port 或 socks5://proxy_host:port
# 注意：使用 SOCKS5 proxy 需要安裝 PySocks: pip install PySocks
# 範例：
# IG_PROXY=http://user:pass@proxy.example.com:8080
# IG_PROXY=socks5://user:pass@proxy.example.com:1080
# IG_PROXY=

